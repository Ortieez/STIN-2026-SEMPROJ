services:
  backend:
    build:
      context: /root/stin/backend
      dockerfile: Dockerfile
    container_name: stin_backend
    restart: unless-stopped
    env_file:
      - /root/stin/backend/.env
    volumes:
      - /root/stin/backend:/app
    ports:
      - "8080:8080"
 
  frontend:
    build:
      context: /root/stin/frontend
      dockerfile: Dockerfile
    container_name: stin_frontend
    restart: unless-stopped
    env_file:
      - /root/stin/frontend/.env
    volumes:
      - /root/stin/frontend:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    depends_on:
      - backend
 